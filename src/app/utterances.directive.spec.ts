import { UtterancesDirective } from './utterances.directive';

describe('UtterancesDirective', () => {
  it('should create an instance', () => {
    const directive = new UtterancesDirective();
    expect(directive).toBeTruthy();
  });
});
